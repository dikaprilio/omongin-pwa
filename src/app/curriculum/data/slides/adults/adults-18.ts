import type { Slide, QuizQuestion } from "../../types";

export const slides: Slide[] = [
    {
        type: 'title',
        title: "Adults Meeting 18",
        subtitle: "Global ID: 18",
        teacherNote: "Update this slide with real content."
    }
];

export const quiz: QuizQuestion[] = [];
